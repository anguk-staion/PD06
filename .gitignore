# 디자인 원본 (웹사이트에 필요 없음)
*.ai
animated (84).webp

# 웹용으로 변환한 원본 — 실제로 쓰는 건 .webp 쪽
# (원본을 교체했다면 아래 명령으로 .webp 를 다시 만들 것)
#   ffmpeg -y -i wallpaper2.png      -c:v libwebp -quality 85 wallpaper2.webp
#   ffmpeg -y -i player.png -vf scale=320:-1:flags=neighbor -c:v libwebp -quality 90 player.webp
#   ffmpeg -y -i 이미지/PD06-01.png  -c:v libwebp -quality 88 이미지/PD06-01.webp
wallpaper2.png
player.png
이미지/PD06-01.png

# 안 쓰게 된 이전 배경 (wallpaper2 로 교체)
wallpaper.png

# 아직 페이지에서 쓰지 않는 소스 아트 (쓰게 되면 이 줄을 지울 것)
head.png
head2.png
Creating_commander.png
이미지/PD06.png

# OS / 편집기 생성 파일
Thumbs.db
desktop.ini
.DS_Store

# 동료 공유용 단독 실행 게임 — 이미지가 전부 내장돼 1.1MB 라 저장소에는 올리지 않는다
# (index.html 배경 게임과는 별개의 파일. 다시 만들려면 스크래치패드의 game-template.html 사용)
playday-game.html

# 위 게임에서만 쓰는 원본 스프라이트 (index.html 은 Airplanes/enemy1/enemy2 만 쓴다)
boss.png
boss2.png
enemy3.png
enemy4.png
